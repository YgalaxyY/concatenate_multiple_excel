# 📊 Excel Multi-Merge Tool
### Простой инструмент для автоматического объединения XLSX-файлов в единый отчет.

Инструмент на базе **Python**, который сканирует рабочую директорию, находит все файлы формата `.xlsx` и консолидирует их содержимое в итоговый документ `report.xlsx`. Идеально подходит для автоматизации ежемесячной отчетности.

---

## 🚀 Как это работает (Workflow)



1. **Сбор данных:** Программа автоматически находит все Excel-файлы в папке запуска.
2. **Исключение:** Скрипт игнорирует сам файл отчета (`report.xlsx`), чтобы избежать зацикливания данных.
3. **Объединение:** Используется библиотека `pandas` для быстрого слияния структур данных.
4. **Запись:** Через `openpyxl` данные вносятся в шаблон, сохраняя структуру ячеек.

---

## 🛠 Технологический стек

* **Pandas** — высокоуровневая обработка и объединение данных.
* **OpenPyXL** — работа с движком Excel для записи финального файла.
* **PySimpleGUI** — легкий графический интерфейс для пользователя.
* **Xlwings** — интеграция с активными книгами Excel.

---

## 📥 Инструкция по использованию

1. Поместите исполняемый файл или скрипт в папку, где лежат ваши `.xlsx` файлы.
2. Убедитесь, что в папке существует файл `report.xlsx` с листом `Sheet1`.
3. Запустите программу.
4. Нажмите кнопку **"Create a new report about clients"**.
5. По завершении статус в окне изменится на **"Results: Complete"**.

---

## 📂 Структура проекта

```text
.
├── main.py              # Исходный код утилиты
├── report.xlsx          # Шаблон итогового отчета (обязателен)
├── data_sample_1.xlsx   # Входящие данные (пример)
└── data_sample_2.xlsx   # Входящие данные (пример)
